apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: project
spec:
  type: ""
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector: 
      app: nginx  

